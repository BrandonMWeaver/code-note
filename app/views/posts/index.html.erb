<% @posts.each do |post| %>
	<div class="display-wrapper">
		<p><%= post.language.name %></p>
		<p><%= post.title %></p>
		<p><%= post.content %></p>
		<p><%= post.description %></p>
	</div>
<% end %>
